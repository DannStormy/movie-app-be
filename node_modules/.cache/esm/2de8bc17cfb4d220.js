let db,Helper,user_queries;_cfb‍.x([["default",()=>UserService]]);_cfb‍.w("../config/config",[["default",["db"],function(v){db=v}]]);_cfb‍.w("../utils/helpers/helpers",[["default",["Helper"],function(v){Helper=v}]]);_cfb‍.w("../queries/user.queries",[["default",["user_queries"],function(v){user_queries=v}]]);



const { registerUser, loginUser, findUserByEmail } = user_queries;

               class UserService {
    /**
     * register new user
     * @memberof UserService
     */
    static async addUser(data) {
        const { firstName, lastName, email, password } = data;
        password = await Helper.generatePasswordHash(password);
        return db.none(registerUser, [firstName, lastName, email, password]);
    }

    /**
    * get user by email
    * @memberof UserService
    */
    static async getUserByEmail(email) {
        return db.oneOrNone(findUserByEmail, [email]);
    }

    /**
     * login user
     * @memberof UserService
     */
    static async loginUser(data) {
        const { email, password } = data;
        return db.oneOrNone(loginUser, [email, password])
    }
}
