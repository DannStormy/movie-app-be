let db,super_queries;_289‍.x([["default",()=>SuperService]]);_289‍.w("../config/config",[["default",["db"],function(v){db=v}]]);_289‍.w("../queries/super.queries",[["default",["super_queries"],function(v){super_queries=v}]]);


const { findSuperByEmail, fetchAllUsers } = super_queries;

               class SuperService {
  /**
   * get super admin by email
   * @memberof SuperService
   */
  static async findSuperByEmail(email) {
    return db.oneOrNone(findSuperByEmail, [email]);
  }

  /**
   * fetch all users
   * @memberof SuperService
   */
  static async fetchAllUsers() {
    return db.many(fetchAllUsers);
  }
}
