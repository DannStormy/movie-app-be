let db,public_queries;_080‍.x([["default",()=>PublicService]]);_080‍.w("../config/config",[["default",["db"],function(v){db=v}]]);_080‍.w("../queries/public_queries",[["default",["public_queries"],function(v){public_queries=v}]]);


const { fetchMoviesByPage, fetchAllMovies } = public_queries;

               class PublicService {
    /**
     * fetch all movies
     * @memberof PublicService
     */
     static async fetchAllMovies() {
        return db.any(fetchAllMovies);
    }

    /**
     * fetch movies by page
     * @memberof PublicService
     */
    static async fetchMoviesByPage(data) {
        let { page } = data
        return db.any(fetchMoviesByPage, [page]);
    }
}
