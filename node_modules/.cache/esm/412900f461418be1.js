let bcrypt,jwt;_614‍.x([["default",()=>Helper]]);_614‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_614‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


               class Helper {
/**
   * hash password
   * @static
   * @memberof Helper
   * @returns {string | number } - generate password hash.
   */
  static async generatePasswordHash(password) {
    const hash = await bcrypt.hashSync(password, 10);
    return hash
  }
/**
   * compare hash password
   * @static
   * @memberof Helper
   * @returns {Boolean}
   */
  static async comparePasswordHash(password, hash) {
    const result = await bcrypt.compare(password, hash);
    _614‍.g.console.log('Result', hash)
    return result;
  }
/**
   * generate user token
   * @static
   * @memberof Helper
   * @returns {String}
   */
  static async generateJWT(payload) {
    const sessionToken = jwt.sign(
        {
            data: payload,
        },
        process.env.JWT_SECRET_KEY, { expiresIn: '1h' }
    );
    return sessionToken
  }



}