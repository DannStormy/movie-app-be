let Helper;_c15‍.x([["default",()=>_c15‍.o]]);_c15‍.w("../utils/helpers/helpers",[["default",["Helper"],function(v){Helper=v}]]);

const { generateJWT } = Helper;

const adminLogin = async (req, res) => {
  try {
    const data = { superID: req.user.id, role: "admin" };
    let token = await generateJWT(data);
    delete req.user.password;
    return res.status(200).json({
      message: "login successful",
      data: { ...token, user: req.user },
    });
  } catch (error) {
    _c15‍.g.console.log(error);
    return error;
  }
};

_c15‍.d(adminLogin);
