let Helper,UserService;_ac5‍.x([["register",()=>register],["login",()=>login]]);_ac5‍.w("../utils/helpers/helpers",[["default",["Helper"],function(v){Helper=v}]]);_ac5‍.w("../services/user_service",[["default",["UserService"],function(v){UserService=v}]]);


const { addUser, loginUser } = UserService
const { generateJWT } = Helper

const register = async (req, res) => {
    try {
        await addUser(req.body)
        return res.status(200).json({
            message: 'register successful'
        })
    } catch (error) {
        _ac5‍.g.console.log(error) //will use logger instead (testing now)
        return error
    }
}

const login = async (req, res) => {
    try {
        await loginUser(req.body)
        let token = await generateJWT(req.body)
        return res.status(200).json({
            message: 'login successful',
            data: token
        })
    } catch (error) {
        _ac5‍.g.console.log(error)
        return error
    }
}

