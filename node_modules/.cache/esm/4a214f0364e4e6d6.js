let express,logger,router;_64e‍.w("dotenv/config");_64e‍.w("express",[["default",["express"],function(v){express=v}]]);_64e‍.w("./src/logger",[["default",["logger"],function(v){logger=v}]]);_64e‍.w("./src/routes",[["default",["router"],function(v){router=v}]]);




const app = express();

const port = process.env.PORT || 1000;

global.logger = logger;
app.use(express.json());
app.use(express.urlencoded({ extended: false }));
app.use(router);

app.listen(3000, () => {
  logger.info(`Application running on port ${port}`);
});
