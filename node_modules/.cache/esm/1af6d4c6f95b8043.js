let db,super_queries;_f95‍.x([["default",()=>SuperService]]);_f95‍.w("../config/config",[["default",["db"],function(v){db=v}]]);_f95‍.w("../queries/super.queries",[["default",["super_queries"],function(v){super_queries=v}]]);


const {
  findSuperByEmail,
  findAdminByEmail,
  fetchAllUsers,
  createAdmin,
  addMovie,
  findUserByID,
  findAdminByID,
  setClientStatus,
  setAdminStatus,
} = super_queries;

               class SuperService {
  /**
   * get super admin by email
   * @memberof SuperService
   */
  static async findSuperByEmail(email) {
    return db.oneOrNone(findSuperByEmail, [email]);
  }

  /**
   * get admin by email
   * @memberof SuperService
   */
  static async findAdminByEmail(email) {
    return db.oneOrNone(findAdminByEmail, [email]);
  }

  /**
   * fetch all users
   * @memberof SuperService
   */
  static async fetchAllUsers() {
    return db.many(fetchAllUsers);
  }

  /**
   * create admin
   * @memberof SuperService
   */
  static async createAdmin(data) {
    const { name, email } = data;
    return db.none(createAdmin, [name, email]);
  }
  /**
   * fetch admin by id
   * @memberof SuperService
   */
  static async fetchAdmin(id) {
    return db.one(findAdminByID, [id]);
  }

  /**
   * add movie
   * @memberof SuperService
   */
  static async addMovie(data) {
    const { title, genre, year } = data;
    return db.none(addMovie, [title, genre, year]);
  }

  /**
   * fetch user by id
   * @memberof SuperService
   */
  static async fetchUser(id) {
    return db.one(findUserByID, [id]);
  }

  /**
   * change user account status
   * @memberof SuperService
   */
  static async setUserStatus(data) {
    const { id, status } = data;
    return db.none(setClientStatus, [status, id]);
  }

  /**
   * change admin account status
   * @memberof SuperService
   */
  static async setAdminStatus(data) {
    const { id, status } = data;
    return db.none(setAdminStatus, [status, id]);
  }
}
