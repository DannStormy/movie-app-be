let db,Helper,user_queries;_03c‍.x([["default",()=>UserService]]);_03c‍.w("../config/config",[["default",["db"],function(v){db=v}]]);_03c‍.w("../utils/helpers/helpers",[["default",["Helper"],function(v){Helper=v}]]);_03c‍.w("../queries/user_queries",[["default",["user_queries"],function(v){user_queries=v}]]);



const { registerUser, loginUser, findUserByEmail } = user_queries;

               class UserService {
    /**
     * register new user
     * @memberof User
     */
    static async addUser(data) {
        let { firstName, lastName, email, password } = data;
        password = await Helper.generatePasswordHash(password)
        return db.none(registerUser, [firstName, lastName, email, password]);
    }

    /**
    * get user by email
    * @memberof User
    */
    static async getUserByEmail(email) {
        return db.oneOrNone(findUserByEmail, [email]);
    }

    /**
     * login user
     * @memberof User
     */
    static async loginUser(data) {
        let { email, password } = data;
        return db.any(loginUser, [email, password])
    }
}
